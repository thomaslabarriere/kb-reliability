[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kb-reliability"
version = "0.1.0"
description = "Layer-attributed reliability diagnostics for a customer-service RAG knowledge base."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "pydantic>=2.6",
  "openai>=1.30",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff>=0.4",
  "mypy>=1.10",
]

[project.scripts]
kb-reliability = "kbreliability.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/kbreliability"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src/kbreliability"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
